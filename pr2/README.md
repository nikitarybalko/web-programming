# Installation

1. Clone repository
2. Open pr2/server folder in terminal
3. Run `npm install` to install all dependencies
4. Run `node server.js` to run the server
5. Open client/index.html with LiveServer extension
6. Done!
